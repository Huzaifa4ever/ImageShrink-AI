fastapi==0.115.0
uvicorn[standard]==0.30.6
motor==3.6.0
pydantic==2.8.2
pydantic-settings==2.4.0
python-multipart==0.0.9
python-dotenv==1.0.1
bcrypt==4.2.1
PyJWT==2.10.1
email-validator==2.2.0
openai==1.82.0
httpx==0.27.2

# Application Insights. Inert unless APPLICATIONINSIGHTS_CONNECTION_STRING is set, so local
# development is unaffected. Auto-instruments FastAPI, httpx and pymongo.
azure-monitor-opentelemetry==1.6.4

# Verifies the signature, audience and expiry of Google Sign-In ID tokens. Getting any of
# those checks wrong turns "sign in with Google" into "sign in as anyone", so this is not
# hand-rolled.
google-auth==2.37.0

# Sends the email-confirmation and password-reset messages.
azure-communication-email==1.0.0
